{
  "name": "example/readonly-ability-plugin",
  "description": "Sample WordPress plugin that registers a read-only ability via Abilities API and exposes it through MCP Adapter.",
  "type": "wordpress-plugin",
  "require": {
    "wordpress/abilities-api": "^0.4 || ^0.5",
    "automattic/jetpack-autoloader": "^5.0"
  },
  "autoload": {
    "psr-4": {
      "ReadonlyAbility\\": "src/"
    }
  }
}
